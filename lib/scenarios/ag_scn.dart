import 'package:flutter/material.dart';

class ag_scn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page 7'),
      ),
      body: Center(
        child: Text('Content for Page 7'),
      ),
    );
  }
}