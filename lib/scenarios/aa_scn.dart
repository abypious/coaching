import 'package:flutter/material.dart';

class aa_scn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page 1'),
      ),
      body: Center(
        child: Text('Content for Page 1'),
      ),
    );
  }
}