import 'package:flutter/material.dart';

class ah_scn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page 8'),
      ),
      body: Center(
        child: Text('Content for Page 8'),
      ),
    );
  }
}